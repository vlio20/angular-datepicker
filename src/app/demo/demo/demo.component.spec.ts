import {DemoComponent} from './demo.component';

describe('Component: Demo', () => {
  it('should create an instance', () => {
    const component = new DemoComponent(null);
    expect(component).toBeTruthy();
  });
});
